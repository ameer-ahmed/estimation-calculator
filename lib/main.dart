import 'package:flutter/material.dart';

void main() {
  runApp(EstimationCalculatorApp());
}

class EstimationCalculatorApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Where we start.
    return Center(
      child: Text('Where we start!'),
    );
  }
}
